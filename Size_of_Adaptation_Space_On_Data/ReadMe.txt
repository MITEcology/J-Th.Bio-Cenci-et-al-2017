#### In this folder we compute the size of the adaptation space on the empirical data
#### We use field number two as an example
#### To run use the file run.sh 
#### It will run the files and plot the resutls
